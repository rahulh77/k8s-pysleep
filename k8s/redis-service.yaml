apiVersion: v1
kind: Service
metadata:
  name: redis
spec:
  type: NodePort
  ports:
    - port: 6379
      nodePort: 30379
      targetPort: 6379
      name: http
  selector:
    app: redis

# apiVersion: v1
# kind: Service
# metadata:
#   name: redis
# spec:
#   ports:
#     - port: 6379
#       targetPort: 6379
#   selector:
#     app: redis
